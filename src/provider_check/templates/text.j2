{{ summary }} - report for domain {{ domain }} ({{ report_time }}) / provider: {{ provider_label }}

{% for result in results %}
{%   if not loop.first %}

{%   endif %}
{{   result.record_type }}: {{   result.status }} - {{   result.message }}
{%   if result.text_rows %}
{{     format_text_row(text_headers, result.text_widths) }}
{%     for row in result.text_rows %}
{{       format_text_row(row, result.text_widths) }}
{%     endfor %}
{%   endif %}
{% endfor %}
